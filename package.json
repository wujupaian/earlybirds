{
  "name": "earlybirds",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "npm --workspace @earlybirds/mobile run start",
    "dev:api": "npm --workspace @earlybirds/functions run emulate",
    "build": "npm run build -w @earlybirds/shared && npm run build -w @earlybirds/functions",
    "typecheck": "npm run typecheck -w @earlybirds/shared && npm run typecheck -w @earlybirds/functions && npm run typecheck -w @earlybirds/mobile"
  }
}
